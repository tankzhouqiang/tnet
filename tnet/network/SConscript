# -*- mode: python -*-
Import('env')
env.MergeFlags('-I.')

libnetwork_sources = [
    'EpollEvent.cpp',
    'Transport.cpp',
    'ServerAdapter.cpp',
    'ClientAdapter.cpp',
    'TcpAcceptor.cpp',
    ]

env.SharedLibrary(target = 'tnet_network',
              source = libnetwork_sources)

SConscript('test/SConscript', 'env')
